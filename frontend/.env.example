# 复制为 .env 后按需修改（.env 勿提交）
# cp .env.example .env

# 开发推荐走 Vite 代理，避免跨域（见 vite.config.ts server.proxy）
VITE_API_BASE_URL=/api/v1

# 代理目标后端地址
VITE_PROXY_TARGET=http://localhost:8080

# true：业务层可走本地 Mock（具体由各 service 决定）
VITE_USE_MOCK=true
