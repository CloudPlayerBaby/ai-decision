feat(ai): 完善异步推演异常拦截与全局事件重构

1. NodeExecutionEvent: 扩充 errorMessage 字段，用于携带大模型报错细节。
2. 节点重构: 对全部 6 个推演节点追加全局 try-catch 拦截。大模型抛出异常时立即广播 FAILED 事件并重新抛出，为 B 组异步状态落库提供完美支持。
3. 基建补全: 补全 BusinessException 类的骨架代码，完美支持传入 ErrorCode 字典，统一后续业务异常拦截规范。
