fix(ai): 补全节点生命周期事件缺失的 taskId 上下文

1. 状态机制：在 WorkflowExecutorImpl 启动时将 taskId 注入 initData，并在 DecisionState 中暴露 getTaskId() 方法。
2. 事件广播：扩展 NodeExecutionEvent 增加 taskId 字段，在全部 6 个执行节点抛出事件时同步传递 taskId。
3. 收益：彻底解决同一 decisionId 多次执行引发的会话污染隐患，使 B 组能按任务实例精准推送 SSE 进度。
